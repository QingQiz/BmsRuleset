# 游玩机制与设置

[返回 README](../README.zh-CN.md) | [English](./gameplay.md)

## 输入与键位

游玩时按**上/下方向键**临时加快或减慢滚速。可在 **设置 → Key Bindings → osu!BMS** 中重新绑定。

每次按键都会播放对应列中下一个音符的按键音，不受判定结果影响。

## 判定与计分

**判定等级（beatoraja 判定窗口，由 `#RANK` / EXRANK 决定）：**

| 名称         | EX 分 | 连击      | RANK 2 (Normal) 窗口    |
|------------|------|---------|-----------------------|
| **PGREAT** | 2    | 保持      | ±15 ms                |
| **GREAT**  | 1    | 保持      | ±45 ms                |
| **GOOD**   | 0    | 保持      | ±112.5 ms             |
| **BAD**    | 0    | 重置      | -220ms / +280ms       |
| **POOR**   | 0    | 重置      | > +280ms              |
| **E-POOR** | 0    | **不中断** | [-500ms,-220ms]，不消耗音符 |

`#RANK` 0 = Very Hard (±5/15/37.5 ms，BAD -220/+280 ms) → 4 = Very Easy (±25/75/187.5 ms，BAD -220/+280 ms)。
`#DEFEXRANK` 和无编号的 `#EXRANK` 以百分比设置初始判定窗口宽度，`100` 等同 Normal（`#RANK 2`）。
`A0` 通道可在谱面中途应用带编号的 `#EXRANKxx`；引用未定义时，窗口保持不变。

**分数：** `总 EX 分 / 最大 EX 分 × 1,000,000`

**DJ LEVEL 评级：** S（全 PGREAT）· AAA ≥ 8/9 · AA ≥ 7/9 · A ≥ 6/9 · B ≥ 5/9 · C 其它

### 判定选择算法

在**设置 → BMS → 判定选择算法**中，可设置同一轨道的判定窗口重叠时，本次按键判定哪个音符。
四种策略对应 beatoraja 的 `Combo`、`Duration`、`Lowest` 和 `Score` 算法：

| 选项 | 选择规则 |
|------|----------|
| **连击优先（LR2）**——默认 | 当前候选音符已严格超过 GOOD 的 Slow 边界，且本次按键不早于后一个音符的 GOOD Fast 边界时，改选后者。 |
| **时间差优先（AC）** | 选择与按键时刻最接近的可判定音符；时间差相等时选择较早的音符。 |
| **最早音符优先** | 选择最早可判定的音符，即使后一个音符能得到更好的判定。 |
| **分数优先** | 与连击优先使用相同的换选规则，但将 GOOD 边界替换为 GREAT 边界。 |

例如，7K `#RANK 2` 谱面的同一键盘轨道在 1000 毫秒和 1100 毫秒各有一个音符。若在 1100 毫秒按键，
连击优先或最早音符优先会判定第一个音符，得到 GOOD；时间差优先或分数优先会判定第二个音符，得到 PGREAT。

选择算法适用于普通音符和长音符头部，包括皿轨。各音符的窗口由布局、RANK/EXRANK 和判定窗口 Mod 决定。
长音符松键规则不变，E-POOR 不消耗音符。

算法在每局开始时固定，设置更改从下一局生效。回放使用录制时的算法，不受当前设置影响；此设置加入前的回放沿用原有算法。

## 血量

通过 Mod 选择血条：6 种常规类型（Groove 或 Survival）和 3 种段位类型，默认使用 Normal。

> [!NOTE]
> 血量数值和算法取自 **beatoraja**（SEVENKEYS 模式），沿用其对 LR2 血条的复现。
> Survival 血条（H1/H2）的回复缩放系数 `(2 × #TOTAL − 320) / notes` 遵循 beatoraja 的 `LIMIT_INCREMENT` 修饰器。
> 地雷伤害使用 BMS 规范公式。

| Mod         | 缩写     | 类型   | 算法              | 初始 HP | 通关    | 血条         |
|-------------|--------|------|-----------------|-------|-------|------------|
| Assist Easy | **E2** | 难度降低 | TOTAL (#TOTAL)  | 20%   | ≥ 60% | Groove（动态） |
| Easy        | **E1** | 难度降低 | TOTAL (#TOTAL)  | 20%   | ≥ 80% | Groove（动态） |
| Normal      | *(默认)* | —    | TOTAL (#TOTAL)  | 20%   | ≥ 80% | Groove（动态） |
| Hard        | **H1** | 难度增加 | Limit Increment | 100%  | 存活    | 固定红色       |
| EX Hard     | **H2** | 难度增加 | Limit Increment | 100%  | 存活    | 固定紫色       |
| Hazard      | **H3** | 难度增加 | Fixed           | 100%  | 存活    | 固定金色       |
| Class       | **C1** | 段位     | Fixed           | 100%  | 存活    | 固定红色       |
| EX Class    | **C2** | 段位     | Fixed           | 100%  | 存活    | 固定紫色       |
| EX Hard Class | **C3** | 段位   | Fixed           | 100%  | 存活    | 固定金色       |

- **Groove 血量**（E2/E1/Normal）：可回复，从 20% 开始，结束时需达到通关线。血条颜色按阈值动态变化：红（< 20%）→ 黄（< 通关线）→
  绿（≥ 通关线）。
- **Survival 血量**（H1/H2/H3）：从 100% 开始，降至 0% 即失败；H3 无回复。血条使用固定颜色，无通关线。
- **段位血条**（C1/C2/C3）：跨曲目保留血量的 Survival 血条，降至 0% 即段位失败，颜色为红、紫或金色。
  所选常规血条会映射到对应的 Class 档位。
- Hard（H1）有 **guts 保护**：低于各血量阈值时减少伤害（低于 50% → ×0.8、低于 40% → ×0.7、……、低于 10% → ×0.4）。
- `#TOTAL` 控制 TOTAL 算法血量的最大回复速度。未指定时，5 键、7 键、PMS 和 LR2 布局使用
  `max(7.605 × N / (0.01 × N + 6.5), 260)`；24 键布局使用 `max(7.605 × (N + 100) / (0.01 × N + 6.5), 300)`（N 为总可玩音符数）。
- 地雷伤害：36 进制值 ÷ 2，单位为百分比（例如 `ZZ` = 647.5% → 直接清空）。

## 灯（Lamp）

曲目面板、段位曲目面板和段位卡片用**通关灯**标记最佳成绩。颜色、闪烁和顺序遵循 **beatoraja** 默认皮肤的
`lamp.png`，沿用 LR2 约定。从低到高依次为：

| 灯        | 填充色   | 闪烁色   | 含义                              |
|----------|--------|-------|---------------------------------|
| NO PLAY  | `#282C30` | —     | 没有成绩记录                          |
| FAILED   | `#E92F0A` | `#0F0300` | 未通过；以 60 ms 周期闪烁               |
| L-ASSIST | `#DDA2DF` | —     | 使用 **Assist Easy** 血条通过          |
| EASY     | `#56CA43` | —     | 使用 Easy 血条通过                    |
| NORMAL   | `#F5C758` | —     | 使用 Normal 血条通过                  |
| HARD     | `#F8F7F5` | —     | 使用 Hard 血条通过                    |
| EX-HARD  | `#EFFD09` | `#FD0909` | 使用 EX-Hard 或 Hazard 血条通过；以 60 ms 周期闪烁 |
| FULL COMBO | `#FFFFFF` | `#09FAFD` | 全连；以 60 ms 周期闪烁                 |
| PERFECT  | `#FFFFFF` | `#3FFF4D` | 仅 PGREAT + GREAT；以 60 ms 周期闪烁    |
| MAX      | `#FFFFFF` | `#FFEB42` | 全 PGREAT；以 60 ms 周期闪烁            |

面板显示与当前 Mods 匹配的最高通关灯和评级。使用更简单的血条通关不会让已有的灯降级。

降低难度时仍会显示在更困难条件下获得的灯；提高难度时则隐藏在更简单条件下获得的灯。
筛选通关灯时，只有 Double Time 会被视为提高难度。

beatoraja 将谱面辅助（**ASSIST**，深紫）与 Assist Easy 血条（**L-ASSIST**）区分。
本规则集没有谱面级辅助选项，因此 Assist Easy 通关获得 L-ASSIST 灯，不产生 ASSIST 灯。

### 段位灯

| 段位层级（血条）   | 灯      |
|------------|--------|
| Class (C1) | NORMAL |
| Ex Class (C2) | HARD |
| Ex Hard Class (C3) | EX-HARD |

段位不产生 FULL COMBO、PERFECT 或 MAX 灯。段位卡片显示与当前 Mods 匹配的最佳灯和评级。

## Mods

| Mod                      | 说明                                       | 选项                     |
|--------------------------|------------------------------------------|------------------------|
| Autoplay                 | 自动播放                                     |     |
| Double Time / Half Time  |                                          | 速度；调整音高             |
| No Fail                  |                                          |     |
| Mirror                   | 镜像键位布局                                   |     |
| 反键 (IN)                  | 将每条轨道中除最后一个音符外的音符转换为长按音符                 | 随机 LN 长度；随机种子          |
| 2P                       | 将玩家布局从 1P 切换为 2P                         |     |
| Constant (CN)            | 禁用滚速变化，包括 #SPEED/#SCROLL/BPM 变化造成的影响 |         |
| Auto Scratch (AS)        | 自动演奏皿轨                                   |     |
| Hide Scratch (HS)        | 移除皿音符并隐藏皿轨                       |     |
| No Mine (NM)             | 移除所有地雷，包括皿轨上的地雷                 |     |
| Background Keysound (BK) | 将按键音作为背景音播放，不再由按键触发                         |     |
| Lane Random (LR)         | RANDOM：随机排列轨道列                           | 包含皿；随机种子；轨道顺序       |
| Note Random (NR)         | S-RANDOM / H-RANDOM：逐音符随机                | 包含皿；模式；随机种子           |
| Rotation Random (RR)     | R-RANDOM：旋转 + 可选镜像                       | 包含皿；随机种子                 |
| Assist Easy Gauge (E2)   | 使用 Assist Easy BMS 血量                    |     |
| Easy Gauge (E1)          | 使用 Easy BMS 血量                           |     |
| Hard Gauge (H1)          | 使用 Hard BMS 血量                           |     |
| EX Hard Gauge (H2)       | 使用 EX Hard BMS 血量                        |     |
| Hazard Gauge (H3)        | 使用 Hazard BMS 血量                         |     |
| Auto Gauge (AG)          | 从最难血量起；失败时降一档                            |     |
| No Good (NG)             | 移除 GOOD 判定窗口                             |     |
| No Great (NE)            | 移除 GREAT 和 GOOD 判定窗口                     |     |
| Long Note (L1)           | LN：仅在尾部判定                       |     |
| Charge Note (L2)         | CN：头尾分别判定                     |     |
| Hell Charge Note (L3)    | HCN：CN 加上长音符持续期间的扣血与回复               |     |

## 设置

| 设置                | 默认值   | 范围 / 选项                    | 说明                                                                                 |
|-------------------|-------|----------------------------|------------------------------------------------------------------------------------|
| 滚动速度 | 8.0 | 1.0–50.0，步长 0.1 | 音符下落速度；游戏中的滚速操作仅临时调整此值。 |
| 基准 BPM            | 主要 BPM | 起始 BPM、最高 BPM、主要 BPM 或最低 BPM | 谱面未声明 `#BASEBPM` 时使用的滚速参考。主要 BPM 取包含可玩音符最多的 BPM；并列时取最早出现者。                  |
| 判定选择算法 | 连击优先（LR2） | 连击优先（LR2）、时间差优先（AC）、最早音符优先、分数优先 | 窗口重叠时，决定本次按键判定哪个音符。详见[选择规则](#判定选择算法)。 |
| BGA 暗化 | 70% | 0%–100% | 0% 保持完整亮度；100% 隐藏 BGA，但不停止播放。 |
| 解除帧率上限           | 关闭    | 开启 / 关闭                    | 在 BMS 游玩期间解除 osu! 的 1000 Hz 帧率与输入轮询上限。更大的 GC 和 GPU 压力可能造成额外卡顿，遇到时请关闭此选项。 |
| 视觉偏移 | 0 毫秒 | -500–500 毫秒，步长 1 毫秒 | 正值让音符提前显示（适合 Slow 较多时），负值让音符延后显示（适合 Fast 较多时）。 |
| LN 尾视觉偏移 | 0 毫秒 | 0–1000 毫秒，步长 1 毫秒 | 让长音符尾部提前显示，使其看起来更短；尾部不会早于头部。 |
| 自动调整视觉偏移 | 关闭 | 开启 / 关闭 | 应用每次有效本地游玩的偏移建议，详见[校准](#视觉偏移校准)。 |
| 使用专用预览音频         | 开启    | 开启 / 关闭                    | 存在 `#PREVIEW` 或 `preview.*` 时优先使用。关闭后，选歌预览仅由 BGM 和按键音样本合成。                        |
| 显示 BMS 5K         | 开启    | 开启 / 关闭                    | 在选歌界面显示或隐藏单人 BMS 5K 谱面。                                                         |
| 显示 BME 7K         | 开启    | 开启 / 关闭                    | 在选歌界面显示或隐藏单人 BME 7K 谱面。                                                         |
| 显示 PMS 9K         | 开启    | 开启 / 关闭                    | 在选歌界面显示或隐藏单人 PMS 9K 谱面。                                                         |
| 显示 BMS 5K DP      | 开启    | 开启 / 关闭                    | 在选歌界面显示或隐藏双人 BMS 5K 谱面。                                                         |
| 显示 BME 7K DP      | 开启    | 开启 / 关闭                    | 在选歌界面显示或隐藏双人 BME 7K 谱面。                                                         |
| 显示 PMS 9K DP      | 开启    | 开启 / 关闭                    | 在选歌界面显示或隐藏双人 PMS 9K 谱面。                                                         |

布局筛选在选歌界面立即生效。

在 BMS 设置中还可导入、清理谱面及[管理难度表](./difficulty-tables.zh-CN.md)。

### 视觉偏移校准

视觉偏移和 LN 尾视觉偏移仅影响显示，不改变音频、判定、计分或按键音。

本地游玩根据击打误差中位数生成建议。可在 BMS 设置中手动应用最近建议的平均值，
或开启**自动调整视觉偏移**，在每次游玩后应用新建议。回放、自动游玩和有效时机判定少于 50 个的游玩不参与校准。

## 选歌搜索

选歌搜索框支持以下 BMS 筛选选项：

| 筛选项 | 简写 | 值 |
|--------|------|----|
| `stars` | `star`、`sr` | osu! 星级难度 |
| `keys` | `k`、`key` | 不包含皿的可玩按键数；例如 `k=7` 会搜索 7K + 皿谱面 |
| `source` | `src` | 谱面来源路径中包含的文本 |
| `ln` | `lns` | LN 物件占总物件的百分比，范围为 0–100 |
| `scratch` | `sc` | 皿轨物件占总物件的百分比，范围为 0–100 |
| `table` | `tb` | 难度表名称或符号 |
| `level` | `lv` | 难度表等级 |

数值筛选支持 `=`、`!=`、`<`、`<=`、`>` 和 `>=`，例如 `ln>=25` 或 `sc<10`。按键数等值筛选还支持逗号分隔，
例如 `keys=5,7`。
