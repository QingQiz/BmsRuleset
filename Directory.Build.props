<!-- Contains required properties for osu!framework projects. -->
<Project>
    <PropertyGroup>
        <ApplicationManifest>$(MSBuildThisFileDirectory)app.manifest</ApplicationManifest>
    </PropertyGroup>
    <PropertyGroup Label="Documentation">
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <NoWarn>$(NoWarn);CS1591</NoWarn>
    </PropertyGroup>
    <PropertyGroup Label="Osu dependency">
        <!-- Shared year.milestone base; bump this to move osu + ruleset together. -->
        <OsuBase Condition="'$(OsuBase)' == ''">2026.624</OsuBase>
        <!-- Ruleset's own "small version". osu's revision is always 0 by convention. -->
        <RulesetRevision Condition="'$(RulesetRevision)' == ''">5</RulesetRevision>
        <!-- Version passed to osu (nuget ref / local checkout target): always base + .0. -->
        <OsuVersion>$(OsuBase).0</OsuVersion>
        <!-- Defaults true: $(OsuVersion) targets a local osu checkout (../osu) because that version
             isn't on nuget.org. Set false (or -p:UseLocalOsu=false) to use the nuget package once published. -->
        <UseLocalOsu Condition="'$(UseLocalOsu)' == ''">true</UseLocalOsu>
    </PropertyGroup>
</Project>
